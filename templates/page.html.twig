<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <header class="header">
            {{ page.header }}
            <br>
            <div class="heading">
                <h1>My Custom Theme</h1>
            </div>
            <nav class="menu">
                {{ page.menu }}
            </nav>
        </header>
        <main class="main">
            <div class="content">
                {{ page.content }}
            </div>
            {% if page.sidebar %}
            <div class="sidebar">
                
            </div>
            {% endif %}
        </main>
        <footer>
            {{ page.footer }}
        </footer>
    </body>
</html>